/**
 * Profile page wrapper
 * Acts as a bridge between router and profile feature component
 */

import { Profile } from '@/features/profile/Profile'

export function ProfilePage() {
  return <Profile />
}
