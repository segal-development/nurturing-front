/**
 * Página de Prospectos
 * Entry point que importa la feature localizada en features/prospectos
 */

import { Prospectos } from '@/features/prospectos/Prospectos'

export default function ProspectosPage() {
  return <Prospectos />
}
